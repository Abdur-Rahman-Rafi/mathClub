{
    "name": "bafskmc-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "postinstall": "cd client && npm install",
        "build": "cd client && npm run build",
        "start": "npx serve -s client/dist"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}